#include<stdio.h>
#include<stdlib.h>
struct node{
	int data;
	struct node *next;
};
int main()
{
	struct node *first;
	struct node *second;
	struct node *third;
	
	head->data = 10;
	head->next = second;
	
	head->data = 11;
	head->next = third;
	
	head->data = 11;
	head->netx = NULL;
}